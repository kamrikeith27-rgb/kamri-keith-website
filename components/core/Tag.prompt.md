Outlined chip for meta attributes — skills, positions, keywords, filters.

```jsx
<Tag>Rise ball</Tag>
<Tag dot>Changeup</Tag>
<Tag active>Pitcher</Tag>
```

`dot` adds a cyan status dot; `active` gives the filled ink state.
